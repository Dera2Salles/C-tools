# File Append

A simple tool to append text to a file.

## Usage
```bash
./file_append <filename> "<text_to_append>"
```
Appends the provided string followed by a newline to the end of the specified file.
