# Dynamic String Array

A demonstration of dynamic memory management with arrays of strings.

## Features
- Uses `malloc`, `realloc`, and `strdup` to manage a list of strings.
- Properly frees allocated memory at the end.
