# File Copy (System Calls)

A low-level file copying utility using Linux system calls (`open`, `read`, `write`).

## Usage
```bash
./file_copy_sys <source_file> <destination_file>
```
Copies the content of the source file to the destination file using a 256-byte buffer.
