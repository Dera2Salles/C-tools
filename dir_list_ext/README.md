# Directory Lister (with Extensions)

A directory listing tool that identifies file extensions.

## Usage
```bash
./dir_list_ext [directory_path]
```
Lists all regular files in the directory and prints their extensions. Defaults to the current directory if no path is provided.
