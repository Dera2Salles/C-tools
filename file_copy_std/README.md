# File Copy (Standard I/O)

A simple file copying utility using standard C library functions (`fopen`, `getc`, `putc`).

## Usage
```bash
./file_copy_std <source_file> <destination_file>
```
Copies the content of the source file to the destination file.
