# Word Replacer

A low-level tool to replace all occurrences of a word with another word in a file.

## Usage
```bash
./word_replacer <filename> <target_word> <replacement_word>
```
Uses a temporary file and low-level I/O to perform the replacement.
