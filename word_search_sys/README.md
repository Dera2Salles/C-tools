# Word Search (System Calls)

A low-level word search tool using system calls.

## Usage
```bash
./word_search_sys [-i] <filename> <word_to_find>
```
Options:
- `-i`: Search for substrings instead of exact matches.

Searches for a word or substring and reports the line numbers where matches are found.
