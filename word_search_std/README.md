# Word Search (Standard I/O)

A simple word search tool using `fgets`.

## Usage
```bash
./word_search_std <filename> <word_to_find>
```
Searches for a specific word in a file line by line and prints the line number if found.
